apiVersion: v1
kind: Secret
metadata:
  name: {{ include "r-orchestrator.fullname" . }}
  labels:
    {{- include "r-orchestrator.labels" . | nindent 4 }}
type: Opaque
data:
  AGENT_TOKEN: {{ .Values.secret.agentToken | b64enc | quote }}
